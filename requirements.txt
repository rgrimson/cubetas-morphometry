# Cubetas — reproducible analysis pipeline (src/)
# Python 3.11. Create and activate a virtual environment, then:
#   pip install -r requirements.txt
#
# These are the only libraries the distributed pipeline (src/01–08, F02, F06)
# imports. Pinned as lower bounds; newer versions are expected to work.

geopandas>=1.0
shapely>=2.0
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.8
pyogrio>=0.7        # GeoPackage I/O backend used by geopandas (list_layers, read/write)

# ---------------------------------------------------------------------------
# rasterio is OPTIONAL. F03_rosarios_tensor.py uses it only to paint its optional
# TPI background, and the non-distributed auxiliary scripts (Figure 1 map,
# covariate/tensor baking) use it to read the proprietary LiDAR DEMs:
#   rasterio>=1.3
# The rest of the pipeline runs without it.
